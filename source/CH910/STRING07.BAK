#include<string.h>
#include<stdio.h>
void main( ){
  char *str;
  char *partstr;
  str= "String^of things,to ,be^split";
  partstr=strtok(str, "^,");
  while(partstr!=NULL)
  { printf("Token found = '%s'\n",partstr);
    partstr=strtok(NULL,"^,");
  }
  }
 //run
 //Token found =
 //Token found =
 //Token found =
 //Token found =
 //Token found =
